import Cta from "./Cta";
import Navigation from "./Navigation";

const HeaderComponent = () => {
    return (
        <header>
            <Cta/>
            <Navigation/>
        </header>
    )
};

export default HeaderComponent;